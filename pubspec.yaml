name: giphy_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.6
  #freezed: ^2.4.5 commented out to avoid version conflicts with bloc_test and mocktail
  json_serializable: ^6.7.1
  bloc_test: ^9.1.0
  mocktail: ^1.0.0


flutter:
  uses-material-design: true
